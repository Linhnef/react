import { Typography } from "@material-ui/core"
import styled from "styled-components"

const TypographyOverline = styled(Typography).attrs((props) => ({ variant: "overline", ...props }))``

export default TypographyOverline
